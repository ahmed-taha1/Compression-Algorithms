public class tag {
    public tag(int position, int length, char next){
        this.position = position;
        this.length = length;
        this.next = next;
    }
    public int position, length;
    public char next;
}
